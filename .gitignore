# 高考志愿狙击手 — .gitignore

# ── 密钥与敏感配置 ──
*.env
!*.env.example

# ── Python ──
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# ── 数据库 ──
*.db
*.sqlite3

# ── Node ──
frontend/node_modules/
frontend/dist/

# ── IDE / OS ──
.DS_Store
.vscode/
.idea/
*.swp
*.swo
.codebuddy/
.cursor/

# ── 运行时日志 ──
*.log
nohup.out

# ── 数据原始文件（体积大, 不随代码提交） ──
backend/data/raw/
backend/data/zszc/

# ── Docker ──
docker-compose.override.yml

# ── 视频与大文件 ──
*.mp4
*.mov
*.avi
frontend/public/models/armor.glb

# ── 临时/备份文件 ──
*.backup
*.bak
*.tmp

# ── 数据修复过程的备份/快照/中间产物（部署不需要，体积大）──
backend/data/_backup_*/
backend/data/_snapshot_*/
backend/data/backup_*/
backend/data/intermediate/
backend/data/*.csv.bak_*
backend/data/_merge_report_*.txt

# ── 审核员临时调试脚本（已含在初始提交的除外）──
backend/scripts/_tmp_*.py
backend/scripts/_peek_*.py
backend/scripts/_sc_*.py

# ── 产品文档（可选，如需排除请取消注释）──
# 报名创意方案.html
# 产品解说.html

# ── 敏感文件 ──
.cursor/
CLAUDE.md
