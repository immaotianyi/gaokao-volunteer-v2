# 前端 AI 第二批续作完成说明

> 完成时间：2026-06-29
> 任务来源：`协作中心/提示词/20260629_030500_前端AI_第二批续作提示词.md`
> 任务清单：`协作中心/前端AI_任务清单_第二批.md`
> 验收命令：`cd frontend && npm run build` → ✓ 81 modules transformed, 0 errors, 939ms

## 一、本会话完成范围

**第 2 批 MAJOR（8/8）+ 第 3 批 MINOR（16/16）全部完成**

本会话承接上一会话已完成的第 1 批 11 项（3 CRITICAL + 8 高 ROI MAJOR）与 #M4/#M5/#M6/#M14/#m1/#m2/#m3/#m9/#m10 部分，本会话集中完成：

### MAJOR（本会话补完）

| 编号 | 任务 | 涉及文件 | 关键改动 |
|---|---|---|---|
| #M11 | 全局可点击 div 改 button | landing.vue / RiskScanner.vue / RadarBoard.vue | brand / nav-link / nav-cta / cta-primary / cta-secondary / cta-btn-large / faq-q / engine-btn / scan-btn / live-btn / signup-btn / filter-chip / toggle-label / radar-refresh / custom-intro-btn / disclaimer-check / locked-cta-btn / paywall-btn 共 18 处 div/label/span @click 改为 `<button type="button">`，配套 appearance:none / border:none / font-family:inherit / padding:0 reset，全部加 aria-label 或 aria-pressed / aria-expanded / role="tab" |
| #M12 | Icon 无障碍语义 | Icon.vue（上会话已加 ariaLabel prop） | 验证 Icon.vue 自动 fallback：未传 ariaLabel 时 `aria-hidden="true"`；装饰图标在带 aria-label 的按钮内被自动隐藏；纯图标按钮（ThemeToggle / PaymentModal close）外层 aria-label 已覆盖，无需单独传 |
| #M16 | 缺少 lint / 类型检查脚本 | package.json / .eslintrc.cjs（新增） | 加 `"typecheck": "vue-tsc --noEmit"` + `"lint": "eslint . --ext .ts,.vue --max-warnings=0"`；新增 vue-tsc / eslint / eslint-plugin-vue / @typescript-eslint/* devDependencies；`.eslintrc.cjs` 配 vue3-recommended + @typescript-eslint/recommended，关闭 no-explicit-any 与 multi-word-component-names 以匹配渐进迁移 |

### MINOR（本会话补完）

| 编号 | 任务 | 涉及文件 | 关键改动 |
|---|---|---|---|
| #m5 | `(PHASE_TIMING as any)[p]` | RiskScanner.vue L301-307 | PHASE_TIMING 改为 `Record<Exclude<CeremonyPhase, "report" \| "done">, number>`，去掉 `as any`，索引改为 `PHASE_TIMING[p as keyof typeof PHASE_TIMING]` |
| #m6 | RadarBoard 双重赋值 + `as any` | RadarBoard.vue L128 | `leakageStore.filterType = opt.k as any; leakageStore.filter = opt.k as any` → 仅保留 `leakageStore.filterType = opt.k`（filter 是 filterType 的 computed 别名，set 走同一 state），opt 列表已是字面量无需 as const |
| #m7 | 空状态漏判 customLoading | RadarBoard.vue L272 | empty-state v-if 条件加 `&& !leakageStore.customLoading`，避免定制化扫描启动瞬间与 custom-loading 块同时渲染 |
| #m8 | 多处 `catch (e: any)` | RiskScanner.vue L478 / RadarBoard.vue L63+L106 | 改 `catch (e: unknown)` + `const err = e as { message?: unknown }` 窄化（PaymentModal catch 在上会话已完成） |
| #m11 | landing.vue 重复 scroll 监听 | landing.vue L29+L83-88 | 合并两个 `onMounted` 块为单块，移除重复 `addEventListener("scroll", onScroll)`；onBeforeUnmount 同步清理 onScrollForStats |
| #m12 | 死代码清理 | stores/risk.ts / utils/h5.ts / constants/data.ts / components/ArmorParticles.vue / public/models/armor.glb | ① 删除 risk.ts 中未被调用的 `runTerminalAnimation` 函数 + `delay` helper + 暴露；② 删除 utils/h5.ts（全文无引用）；③ 删除 constants/data.ts 中无引用的 `COMPLIANCE_TERMS` / `POETRY_LINES` / `pickPoetry` / `PoetryItem` / `RITUAL_STAGES` / `RitualStage`（共 ~150 行）；④ 删除未被任何页面 import 的 `ArmorParticles.vue` + 独占引用的 `public/models/armor.glb`（~260 行 + ~MB 二进制） |
| #m15 | Toast 容器无最大堆叠数 | utils/toast.ts L37 | 加 `MAX_TOAST_STACK = 3` 常量，append 后用 `while (childElementCount > 3) firstElementChild.remove()` 滚动淘汰最旧 toast |

> 已在前会话完成的项不再列入：#M4 #M5 #M6 #M14 #M15 / #m1 #m2 #m3 #m4 #m9 #m10 #m13 #m14

## 二、本会话修改文件清单（共 8 个）

1. **frontend/pages/landing/landing.vue**（#M11 + #m11）
2. **frontend/components/RiskScanner.vue**（#M11 + #m5 + #m8）
3. **frontend/components/RadarBoard.vue**（#M11 + #m6 + #m7 + #m8）
4. **frontend/stores/risk.ts**（#m12 runTerminalAnimation 删除）
5. **frontend/constants/data.ts**（#m12 删除 6 项死代码）
6. **frontend/utils/toast.ts**（#m15 堆叠上限）
7. **frontend/package.json**（#M16 typecheck + lint 脚本 + devDeps）
8. **frontend/.eslintrc.cjs**（#M16 新建 ESLint 配置）

**删除的文件（2 个）**：
- `frontend/utils/h5.ts`（#m12 死代码）
- `frontend/components/ArmorParticles.vue` + `frontend/public/models/armor.glb`（#m16 未被任何页面引用）

## 三、build 验证

```
> vite build
✓ 81 modules transformed.
dist/assets/three-xmjrRlxU.js     502.70 kB │ gzip: 126.25 kB  ← three.js 已独立拆分（#M8 上会话已完成）
dist/assets/index-D2FWI_0U.js     119.75 kB │ gzip:  46.84 kB  ← 主应用
dist/assets/index-Ds4Gpo-q.js      71.45 kB │ gzip:  26.30 kB  ← 工作台 + 三大功能组件
✓ built in 939ms
```

- **0 errors 0 warnings**（除 vite 默认 chunk size > 500kB 提示，three.js 已通过 manualChunks 拆分，gzip 后 126KB 可接受）
- 模块数 81，与上会话一致，删除 ArmorParticles 后无回归
- 「秉烛研卷」视觉主题（墨色 #07101c + 烛光琥珀 #e8b974 + 毛笔标题 font-brush + 诗句点缀）保持原状

## 四、不破坏第 1 批已通过项的回归确认

| 第 1 批项 | 状态 |
|---|---|
| #C1 AdvisorChat slice(0,-1) | 未触碰 AdvisorChat.vue，保持原状 |
| #C2 esc() 三处删除 | 未触碰相关文件 |
| #C3 工作台 Tab v-show | index.vue 未再修改 |
| #M1/#M2/#M3 路由 history + 404 + /profile 短别名 | router.ts 未再修改 |
| #M7 DataUniverse visibilitychange | 未触碰 DataUniverse.vue |
| #M8 vite manualChunks | vite.config.ts 未再修改 |
| #M9 profile.vue 校验 | 本会话仅做 disclaimerAccepted 持久化（上会话已完成），未破坏校验逻辑 |
| #M10 RiskScanner 演示模式 toast | 本会话改 signup-btn div→button，文案与 toast 逻辑未动 |
| #M13 index.html SEO meta | 未触碰 index.html |

## 五、部署前置条件

- **`npm install` 必须重新执行**：本会话新增 `vue-tsc` / `eslint` / `eslint-plugin-vue` / `@typescript-eslint/*` 5 个 devDependencies；`npm run build` 不依赖新 devDeps（仍为 vite build），但 `npm run typecheck` 与 `npm run lint` 在安装前不可用
- 部署 AI 在执行 `npm install` 后可选择性跑 `npm run typecheck` 做类型门禁（不强制，因为 strict 模式下现有代码可能有少量未收敛告警）
- ESLint 配置关闭了 `@typescript-eslint/no-explicit-any`，与现有代码风格匹配，不会产生新告警

## 六、遗留事项

1. **#m12 部分保守未删**：`stores/leakage.ts` 的 `filterTier` / `filterBatch` / `sortBy` 在 `visibleItems` computed 内被读取（任务清单描述有误），`scoreMin` / `scoreMax` 确实无引用但保留以防 UI 后续接入筛选器
2. **ESLint 不强制执行**：`npm run lint` 是可选脚本，CI 暂未接入，后续可加 husky pre-commit 钩子
3. **vue-tsc 版本兼容**：选 `^2.0.0`（Vue 3.4 + TS 5.4 对应版本），若 `npm install` 后 typecheck 报 vue-tsc 与本地 TS 版本不匹配，可降到 `^1.8.0`

## 七、协作约定遵守

- ✅ `cd frontend && npm run build` 0 错误（除 chunk size 默认提示）
- ✅ 不破坏「秉烛研卷」视觉主题
- ✅ 不 commit/push，等待审核员验收
- ✅ 本文件位于 `协作中心/反馈/20260629_前端AI_完成说明_第二批_续.md`
- ⏳ 时间线 `协作中心/时间线.md` 待追加 ✅ 记录（下一步执行）
