# 前端 AI 联调验证报告 — #8 选科回显 + #9 career_risk/ai_risk

> 验证时间：2026-06-27 11:15:43
> 执行人：前端 AI
> 触发信号：时间线 `[后端AI] ✅ 完成 #8 #9`（10:41）+ `[审核员] 🔍 验收`（11:08）

## 验证环境

- 后端：http://localhost:8000（uvicorn，含 #8/#9 修复）
- 后端健康：`{"status":"ok","version":"0.5.0","redis":"unavailable","agent":true}`
- 前端：http://localhost:5173（vite dev）
- 验证方式：curl 模拟前端 API 调用 + 代码链路审查

---

## #8 选科成绩回显 — 🔍 联调通过

### 验证步骤

1. **POST /api/profile** 带选科成绩（physics=95 / chemistry=88 / biology=92）
2. **GET /api/profile/{user_id}** 验证回显

### 实测结果

```
测试 user_id: fe_test_1782530057

POST 返回（入库确认）：
  physics_score: 95, chemistry_score: 88, biology_score: 92
  history_score: null, geography_score: null, politics_score: null

GET 回显：
  province: 广东
  score: 565
  subjects: 物理,化学,生物
  physics_score: 95    ✅
  chemistry_score: 88  ✅
  biology_score: 92    ✅
  history_score: None  ✅（未填科目为 null，符合预期）
  geography_score: None ✅
  politics_score: None  ✅

选科回显: PASS
```

### 前端代码链路确认

- `frontend/stores/profile.ts` L29-L35：`UserProfile` 接口已定义 6 个选科字段
- `frontend/stores/profile.ts` L135：`saveProfile({ ...profile.value, user_id })` 发送完整档案含选科成绩
- `frontend/stores/profile.ts` L153：`loadFromBackend` 从 GET 恢复档案

### 结论

✅ **#8 联调通过**。POST→GET 选科成绩完整回显，未填科目为 null 符合预期。前端 saveProfile/loadFromBackend 链路正确消费后端字段。

---

## #9 career_risk/ai_risk 标签 — 🔍 联调通过

### 验证步骤

1. 确认后端 RiskCheckItem schema 含 career_risk/ai_risk
2. curl POST /api/check-risk 测试翻译专业（AI 替代敏感）
3. 确认前端 RiskScanner.vue 标签渲染逻辑

### 实测结果

**后端 schema 确认**（openapi.json）：
```
RiskCheckItem.properties 含 career_risk: True
UserProfileBase.properties 选科字段: ['physics_score','chemistry_score','biology_score','history_score','geography_score','politics_score']
```

**POST /api/check-risk 返回**（广东外语外贸大学 · 翻译）：
```json
{
  "total": 1,
  "results": [{
    "university": "广东外语外贸大学",
    "major": "翻译",
    "status": "PASS",
    "reason": "根据广东外语外贸大学2026年招生章程，该专业未发现体检/单科/语种特殊限制。",
    "matched_clause": "广东外语外贸大学2026年招生章程",
    "career_risk": null,
    "ai_risk": null
  }]
}
```

- ✅ `career_risk` / `ai_risk` **字段存在**（schema 透传链路通）
- 当前值为 `null`：mock 规则引擎不评估就业/AI 风险（符合任务清单"无 LLM/mock 模式时标签不显示，符合预期，不报错"）

### 前端代码链路确认

- `frontend/stores/risk.ts` L25/L27：`RiskResult` 接口已定义 `career_risk?: string | null` / `ai_risk?: string | null`
- `frontend/components/RiskScanner.vue` L651/L665/L680：三处结果卡片（PASS/WARNING/DANGER）均有渲染逻辑
  ```vue
  <div v-if="item.career_risk || item.ai_risk" class="risk-tags-row">
    <span v-if="item.career_risk" class="risk-tag" :class="'risk-' + item.career_risk">
      就业 {{ item.career_risk === 'high' ? '高风险' : item.career_risk === 'medium' ? '中等' : '低风险' }}
    </span>
    <span v-if="item.ai_risk" class="risk-tag" :class="'risk-' + item.ai_risk">
      AI替代 {{ item.ai_risk === 'high' ? '高风险' : ... }}
    </span>
  </div>
  ```
- `v-if="item.career_risk || item.ai_risk"` → null 时不渲染（符合预期）
- 真实 LLM 返回 `'high'/'medium'/'low'` 时标签显示 + 对应 class 配色

### 结论

✅ **#9 联调通过**。
- 后端 schema 含字段 + 透传链路通（字段在返回中存在）
- 前端类型定义就绪（RiskResult L25/L27）
- 前端渲染逻辑就绪（三处 v-if + 三元判断 high/medium/low）
- mock/null 时不显示不报错（符合预期）
- 真实 LLM 返回非 null 值时标签正常显示（代码链路已验证，生产环境真实 LLM 触发即可生效）

---

## 自检清单

- [x] #8 POST /api/profile 带选科成绩 → 后端正确入库
- [x] #8 GET /api/profile/{user_id} → 选科成绩完整回显
- [x] #8 未填选科为 null（符合预期）
- [x] #9 后端 RiskCheckItem schema 含 career_risk/ai_risk
- [x] #9 POST /api/check-risk 返回含两字段（透传通）
- [x] #9 前端 RiskResult 类型定义就绪
- [x] #9 前端 RiskScanner.vue 三处标签渲染逻辑就绪
- [x] #9 mock/null 时不显示不报错（符合预期）

## 备注

- 当前后端 redis 不可用（`redis: unavailable`），不影响 #8/#9 联调（profile 走 SQLite，risk 走 mock 规则引擎）
- 真实 LLM 场景的 #9 标签显示需上生产或配 DEEPSEEK_API_KEY 后验证，但代码链路已通过审查确认正确
