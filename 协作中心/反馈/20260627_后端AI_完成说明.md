# 后端 AI 完成说明

> 完成时间：2026-06-27 10:41:00（#1-#10）/ 11:18:00（#12 追加）| 执行人：后端 AI
> 任务数：11 项（2 critical / 7 major / 2 minor）— 全部完成
> 项目根：`/Users/sanzhaibanniang/Claude/Projects/gaokao`

## 自检结果

| 检查项 | 结果 |
|---|---|
| `python3 -c "import main"` | ✅ 无 import 错误 |
| `uvicorn main:app --port 8000` 启动 | ✅ 启动正常（135 章程 + 283129 录取数据 + Redis 降级） |
| `curl /health` | ✅ `{"status":"ok","version":"0.5.0","redis":"unavailable","agent":true}` |
| `curl /api/leakage-radar` (广东 565 物理类) | ✅ 返回 66 条 opportunities |
| `kb.query()` 返回 rules_text | ✅ 已收录/未收录两条路径均非空，无 RecursionError |
| POST + GET `/api/profile` 选科成绩回显 | ✅ physics_score:95 / chemistry_score:88 / biology_score:92 全部入库回显 |
| RiskCheckItem 含 career_risk / ai_risk | ✅ schema 字段就位 |
| risk_agent router 兼容 DEEPSEEK_API_KEY | ✅ 两处判断均改为 `LLM_API_KEY or DEEPSEEK_API_KEY` |
| #12 score_rank 多省隔离 | ✅ 广东 600→3367 / 河南 600→32622 / 默认广东 3367 / 12 省独立缓存 |

---

## 逐项修复明细

### #1 支付回调验签堵漏（CRITICAL）— ✅ 验证通过

**文件**：`backend/services/wechat_pay.py`、`backend/routers/payment.py`

**改法**：
1. `wechat_pay.py` 新增 `_signature_strict()` 内部函数：`ENV == "prod"` 时返回 True，否则 False
2. `wechat_pay.py` 模块加载时检查 `is_configured() and not WECHAT_PLATFORM_CERT_PATH`，打印 WARN 日志
3. `wechat_pay.py` `_verify_signature` 在证书路径缺失时：严格模式返回 False，非严格模式维持原 `return True`（mock 兼容）
4. `payment.py` `/api/pay/notify` 的 `except ValueError` 分支：严格模式 `raise HTTPException(403)`，非严格模式维持原 `return {"code":"FAIL"}`

**关键行号**：
- `wechat_pay.py` L61-68（`_signature_strict` + WARN 日志）
- `wechat_pay.py` L232-238（`_verify_signature` 严格模式分支）
- `payment.py` L320-324（notify 403 分支）

**验收**：mock 解锁走 poll 端点（未动），ENV=prod 且证书缺失时 /notify 返回 403。

---

### #2 advisor 异步化 Tavily 调用（MAJOR）— ✅ 验证通过

**文件**：`backend/services/advisor.py`

**改法**：L261 将同步调用 `tavily_search(...)` 包装为 `await asyncio.to_thread(tavily_search, query + " 高考志愿 2026", 3)`。

**参照样板**：`backend/services/live_search.py` L515-L517 的 `asyncio.gather + to_thread` 写法。

**验收**：import 通过，触发顾问联网搜索不再卡死事件循环。

---

### #3 邮件发送异步化（MAJOR）— ✅ 验证通过

**文件**：`backend/services/notification.py`

**改法**：
1. 顶部补 `import asyncio`（L11）
2. 新增同步函数 `_smtp_send_sync(msg_str, smtp_config)`（L248-260）：用 `smtplib.SMTP` 上下文管理器 + `starttls` + `login` + `send_message`，单次连接完成发送，异常捕获并打印日志
3. `send_daily_email` 中改为 `await asyncio.to_thread(_smtp_send_sync, msg.as_string(), smtp_config)`（L313）

**验收**：import 通过，send_daily_email 不再阻塞事件循环；行为不变（保留 try/except 与日志）。

---

### #4 统一差异追踪 key 格式（MAJOR）— ✅ 验证通过

**文件**：`backend/routers/leakage.py`

**改法**：
1. 文件顶部新增公共函数 `_diff_key(university_name, major_name, group_code="") -> str`，返回 `f"{university_name}|{major_name}|{group_code}"`（L60-62）
2. 缓存命中路径（L203）：`set(_diff_key(o.get("university_name",""), o.get("major_name",""), o.get("group_code","")) for o in data.get("opportunities", []))`
3. 新算路径（L282-283）：`current_keys.add(_diff_key(item.get("university_name",""), item.get("major_name",""), item.get("group_code","")))`

**验收**：两条路径 key 格式完全一致，`new_since_yesterday` 计数可比；leakage-radar 接口返回正常。

---

### #5 heat_tracker 收藏数下限保护（MAJOR）— ✅ 验证通过

**文件**：`backend/services/heat_tracker.py`

**改法**：`unfav` 分支用 Lua 脚本替代 `pipe.decr(fav_key)`（L59-69）：
```python
UNFAV_LUA = """
local cur = tonumber(redis.call('GET', KEYS[1]) or '0')
if cur > 0 then
    return redis.call('DECR', KEYS[1])
else
    return 0
end
"""
pipe.eval(UNFAV_LUA, 1, fav_key)
```

内存路径（L88-89 `max(0,...)`）保持不变。

**验收**：import 通过；连续 unfav 不存在的 key，fav_count 恒为 0 不为负。

---

### #6 热度追踪 Session 上下文管理（MAJOR）— ✅ 验证通过

**文件**：`backend/routers/leakage.py`

**改法**：两处 `db = SessionLocal()` 改为 `with SessionLocal() as db:`，移除手动 `db.close()`：
1. `track_heat` 持久化逻辑（L693-706）
2. `heat_stats_overview` 持久化逻辑（L760-774）

`import` 仍在函数内（避免循环导入）。

**验收**：db.add/commit 抛异常时 session 正确关闭，无泄漏；leakage-radar 接口返回正常。

---

### #7 enrollment_kb.query 返回 rules_text（MAJOR）— ✅ 验证通过

**文件**：`backend/services/enrollment_kb.py`

**改法**：
1. `query()` result 初始化增加 `"rules_text": ""`（L140）
2. 第一个 return 出口（L149-153，大学未收录走 `_apply_default_rules` 分支）：补 `result["rules_text"] = self.get_rule_summary(university, major, _pre_built=result)`
3. 末尾 return 出口（L200-206）：补 `result["rules_text"] = self.get_rule_summary(university, major, _pre_built=result)`
4. **递归规避**：`get_rule_summary` 增加可选参数 `_pre_built: dict | None = None`（L317-325），传入时直接复用 result dict，跳过内部 `self.query()` 调用。外部既有调用 `kb.get_rule_summary("X","Y")` 不受影响（保持原签名兼容）。

**验收**：
- `kb.query("暨南大学","临床医学")` → rules_text_len=131（含真实章程条款 + 体检限制 + 条款依据）
- `kb.query("某虚构大学XYZ","临床医学")` → rules_text_len=65（走默认规则，含"色弱者，该专业可不予录取"）
- `kb.get_rule_summary("暨南大学","临床医学")` 外部直接调用 → summary_len=131，兼容
- 无 RecursionError

---

### #8 补齐选科成绩字段（MAJOR，触发前端联调）— ✅ 验证通过

**文件**：`backend/schemas.py`、`backend/models.py`、`backend/main.py`

**改法**：
1. `schemas.py` `UserProfileBase` 补 6 字段（L21-26）：`physics_score / chemistry_score / biology_score / history_score / geography_score / politics_score`，均 `Optional[int] = Field(None, ge=0, le=100)`
2. `models.py` `UserProfile` ORM 补 6 列（L22-27）：`Column(Integer, nullable=True)`
3. `main.py` lifespan 的 `Base.metadata.create_all` 之后（L54-71）加 SQLite ALTER TABLE 补丁：6 列逐个 `ALTER TABLE user_profiles ADD COLUMN {col} INTEGER`，每列 try/except 忽略"列已存在"，外层 try/except 不影响主流程

**验收**：
- POST `/api/profile` 带 `physics_score=95, chemistry_score=88, biology_score=92` → 入库成功
- GET `/api/profile/selftest_008` → 回显 `"physics_score":95, "chemistry_score":88, "biology_score":92, "history_score":null, "geography_score":null, "politics_score":null`
- ALTER TABLE 补丁在已有 SQLite 上执行无异常

---

### #9 补齐 career_risk/ai_risk 字段（MAJOR，触发前端联调）— ✅ 验证通过

**文件**：`backend/schemas.py`、`backend/services/risk_checker.py`、`backend/services/risk_agent.py`

**改法**：
1. `schemas.py` `RiskCheckItem` 补 2 字段（L204-205）：`career_risk: Optional[str] = Field(None, description="就业风险提示")` / `ai_risk: Optional[str] = Field(None, description="AI替代风险提示")`
2. `risk_checker.py` `_call_deepseek` 返回 dict 时透传（L340-341）：`"career_risk": result.get("career_risk")` / `"ai_risk": result.get("ai_risk")`
3. `risk_agent.py` `AuditResult` dataclass 补字段（L52-53）：`career_risk: str = ""` / `ai_risk: str = ""`

**验收**：`RiskCheckItem.model_fields` 含 `career_risk` / `ai_risk`；真实 LLM 返回时透传，Mock 路径保持 None（前端 falsy 不渲染标签，符合预期）。

---

### #10 risk_agent API key 兼容（MINOR）— ✅ 验证通过

**文件**：`backend/routers/risk_agent.py`

**改法**：L216 和 L271 两处 provider 判断改为：
```python
if provider in ("openai", "deepseek") and not (_os.getenv("LLM_API_KEY") or _os.getenv("DEEPSEEK_API_KEY")):
```

**参照**：`backend/services/advisor.py` L53 的 `os.getenv("LLM_API_KEY") or os.getenv("DEEPSEEK_API_KEY")` 写法。

**验收**：源码同时检查两个 env；仅配 `DEEPSEEK_API_KEY` 时 V4 Agent 走真实 LLM 而非 Mock。

---

### #12 score_rank 缺省份过滤，多省数据追加后位次串省（CRITICAL）— ✅ 验证通过

**文件**：`backend/services/score_rank.py`、`backend/routers/score_rank.py`、`backend/schemas.py`

**背景**：项目正在推进全国高考数据爬取（`backend/prompts/`，P0: 甘/豫/陕/川，P1: 皖/渝/闽/冀/鄂/湘/赣/辽）。所有 `yifenyiduan_*.csv` 是多省共享追加模式。审核员 10:12 追加 #12 作为 CRITICAL 前置任务：score_rank 服务不区分省份，爬虫追加新省数据后全站分数-位次转换立刻串省出错。

**实际数据状态**：重启后日志显示 12 省一分一段表已就位（广东/江西/甘肃/陕西/安徽/湖南/河南/重庆/辽宁/四川/河北/福建，2024-2026 三年），#12 修复前确实会串省。

**改法**：
1. `services/score_rank.py`：
   - `_load_table(year, subject_group, province="广东")`：加 province 参数，缓存 key 改为 `(year, subject_group, province)`，过滤改为 `df[(df["subject_group"]==subject_group) & (df["province"]==province)]`
   - 若 CSV 缺 `province` 列直接抛 `ValueError`（防御性）
   - `batch` 过滤（本科批）保留
   - `score_to_rank` / `rank_to_score` / `get_score_range` 全部增加 `province: str = "广东"` 参数（默认值保持兼容）
   - `preload_all()` 改为遍历每个 CSV 的 `unique province` 逐个预加载，日志改为 `[ScoreRank] 已加载 {year}年 {province} {subject} 一分一段表`
2. `schemas.py`：
   - `ScoreRankRequest` 增加 `province: str = Field("广东", min_length=2, max_length=20, description="省份（多省数据隔离）")`
   - `ScoreRankResponse` / `ScoreRangeResponse` 的 `province` 字段保留默认 "广东"（路由层传实际值覆盖）
3. `routers/score_rank.py`：
   - `GET /convert`、`POST /convert`、`GET /range`、`GET /reverse` 全部增加 `province: str = Query("广东", min_length=2, max_length=20)` 查询参数
   - `_do_convert` 增加 `province` 参数透传，调用 `score_to_rank(score, year, subject_group, province)`
   - 响应的 `province` 字段用实际传入值（不再硬编码 "广东"）

**关键行号**：
- `services/score_rank.py` L19（缓存 key 注释）、L22-51（`_load_table`）、L54-67（`score_to_rank`）、L83-96（`rank_to_score`）、L106-130（`get_score_range`）、L156-178（`preload_all`）
- `schemas.py` L292（`ScoreRankRequest.province`）
- `routers/score_rank.py` L28-31（GET /convert）、L37（POST /convert）、L40-54（`_do_convert`）、L63-67（GET /range）、L92-96（GET /reverse）

**验收**（服务层 + HTTP 接口双重验证）：
- 服务层：广东 2026 物理类 600 分 → 位次 3367（与改前一致，回归通过）；河南 2026 物理类 600 分 → 位次 32622（隔离成功）；缓存 key `(2026, '物理类', '广东')` 570 行 vs `(2026, '物理类', '河南')` 607 行独立；反查广东位次 3367 → 分数 600 正确；`get_score_range` 广东 rank=13190 vs 河南 rank=73451 隔离；不传 province 默认广东 3367 向后兼容
- HTTP：`GET /api/score-rank/convert?score=600&province=广东` → `{"rank":3367,"province":"广东"}`；`?province=河南` → `{"rank":32622,"province":"河南"}`；不传 province → `{"rank":3367,"province":"广东"}`
- uvicorn 启动日志确认 12 省 × 2 科类 × 3 年 = 72 个独立缓存条目，无串省

**数据扩展解锁**：#12 完成后，爬虫脚本可安全向 `yifenyiduan_*.csv` 追加新省数据。

---

## 既有约束遵守情况

- ✅ 不破坏既有接口：uvicorn 重启后 `/health` / `/api/leakage-radar` / `/api/profile` 均正常
- ✅ 修复最小化，未引入未要求的抽象/重构
- ✅ `live_search` 相关用 `asyncio.gather + to_thread`（#2 参照样板）
- ✅ 志愿草表输入校验、"分数匹配"7 策略、`score_tolerance` 默认 30 等既有逻辑未动
- ✅ 支付 mock 模式行为不变（#1 仅给生产模式加严格门禁）
- ✅ `get_rule_summary` 外部调用签名兼容（#7 仅加可选参数）

## 后端服务状态

- uvicorn 运行中：`http://localhost:8000`
- 前端 AI 可基于 #8 / #9 完成信号进行联调验证
