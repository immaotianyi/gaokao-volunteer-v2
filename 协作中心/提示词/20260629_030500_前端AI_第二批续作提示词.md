# 前端 AI 第二批续作提示词（第 2/3 批）

> 生成时间：2026-06-29 03:05:00 | 生成人：审核员 | 发送对象：前端 AI
> 上游：[前端AI_任务清单_第二批.md](../前端AI_任务清单_第二批.md)
> 当前状态：第 1 批 11 项已通过审核员验收（未 commit），第 2 批 8 项 + 第 3 批 16 项共 24 项未做

---

以下是可直接粘贴给前端 AI 的提示词（复制下方代码块内容）：

---

```
你是【前端 AI】，负责高考志愿狙击手项目（Trae AI 创意大赛参赛作品）的前端开发。项目路径：/Users/sanzhaibanniang/Claude/Projects/gaokao

## 当前进度

第 1 批 11 项（3 CRITICAL + 8 高 ROI MAJOR）已通过审核员验收，但改动尚未 commit/push（保留在工作区）。详情见：
- 完成说明：/Users/sanzhaibanniang/Claude/Projects/gaokao/协作中心/反馈/20260628_前端AI_完成说明_第二批.md
- 时间线最新条目：[2026-06-28 23:50:00][前端AI] ✅ 完成第二批第 1 批 11 项

审核员实测确认第 1 批代码已落地：
- #C1 AdvisorChat slice(0,-1) / #C2 三处 esc() 函数全删 / #C3 v-show 替代 v-if
- #M1 createWebHistory / #M2 catch-all 404 / #M3 /profile 短别名
- #M7 visibilitychange + prefers-reduced-motion / #M8 manualChunks three / #M9 validationError / #M10 演示模式标注 / #M13 SEO meta + favicon

## 你现在的任务

继续完成第 2 批 8 项 MAJOR + 第 3 批 16 项 MINOR，共 24 项。任务详情在：
/Users/sanzhaibanniang/Claude/Projects/gaokao/协作中心/前端AI_任务清单_第二批.md

### 立即执行的 3 步

1. 读任务清单：/Users/sanzhaibanniang/Claude/Projects/gaokao/协作中心/前端AI_任务清单_第二批.md
2. 读协作公约：/Users/sanzhaibanniang/Claude/Projects/gaokao/协作中心/协作公约.md
3. 按 ROI 顺序执行第 2 批 8 项 → 然后一次性扫第 3 批 16 项 MINOR

### 第 2 批 8 项（MAJOR，按推荐顺序）

1. **#M4** [App.vue L15](../../frontend/App.vue#L15) `<router-view>` 包 `<Suspense>` + fallback，懒加载 chunk 网络失败时显示重试 UI
2. **#M5** [api/index.ts L20-L39](../../frontend/api/index.ts#L20-L39) `request<T>` 内置 `AbortController` + 默认 30s 超时，允许 caller 传 signal；PaymentModal `pollPaymentStatus` 加最大次数（如 30 次 = 60s）
3. **#M6** [api/index.ts L256-L303](../../frontend/api/index.ts#L256-L303) `startAdvisorStream` 走 `ReconnectingSSE`（与 `startRiskStream`/`startAgentStream` 行为对齐），抽公共重连逻辑
4. **#M11** 全局可点击 `div` 改 `<button>`（配合 CSS reset 去掉默认样式），覆盖文件：
   - [landing.vue L176-L188-L230-L234](../../frontend/pages/landing/landing.vue#L176)
   - [pages/index/index.vue L94-L100](../../frontend/pages/index/index.vue#L94)
   - [RiskScanner.vue L509-L513](../../frontend/components/RiskScanner.vue#L509)
   - [RadarBoard.vue L128-L150-L220](../../frontend/components/RadarBoard.vue#L128)
   - [ProfilePanel.vue L45](../../frontend/components/ProfilePanel.vue#L45)
   若不便全改，至少补 `role="button" tabindex="0" @keyup.enter="..."`
5. **#M12** [Icon.vue L70-L82](../../frontend/components/Icon.vue#L70-L82) 加 `ariaLabel?: string` prop：有则 `role="img" aria-label`，无则 `aria-hidden="true"`；ThemeToggle / PaymentModal 关闭按钮等图标按钮补 `aria-label`
6. **#M14** [stores/profile.ts L19-L38](../../frontend/stores/profile.ts#L19-L38) `UserProfile` 加 `[key: string]: number | string | null | undefined` 索引签名，去掉 [profile.vue L213-L218](../../frontend/pages/profile/profile.vue#L213) 和 [ProfilePanel.vue L94](../../frontend/components/ProfilePanel.vue#L94) 的 `as Record<string, ...>` 断言
7. **#M15** [App.vue L14-L16](../../frontend/App.vue#L14-L16) `<router-view>` 包 `<Transition name="page-fade" mode="out-in">`，CSS 加 `.page-fade-enter-active` 等过渡类（与既有 `.guide-fade` 风格协调）
8. **#M16** [package.json L5-L9](../../frontend/package.json#L5-L9) 加 `"typecheck": "vue-tsc --noEmit"`，引入 ESLint + `eslint-plugin-vue`（最小配置即可）

### 第 3 批 16 项 MINOR（一次性扫掉）

任务清单第 250-351 行列了 #m1-#m16 全部细节，包括：
- 类型修复：#m1 ProfileSaveResponse / #m2 Omit<UserProfile,"user_id"> / #m4 headers 合并 bug / #m5 PHASE_TIMING Record 类型 / #m6 RadarBoard 双重赋值 / #m8 catch(e:unknown) / #m9 ProfileEditor :style as / #m14 profile.vue 表单响应 store
- 死代码清理：#m3 假 EventSource readyState / #m12 stores/risk.ts `runTerminalAnimation`、utils/h5.ts、leakage store 部分state、constants/data.ts 中的 `COMPLIANCE_TERMS/POETRY_LINES/pickPoetry/RITUAL_STAGES`
- UI 修复：#m7 RadarBoard 空状态漏判 customLoading / #m10 profile.vue disclaimerAccepted / #m11 landing.vue 重复 scroll 监听 / #m13 index.html viewport-fit=cover（注：第 1 批 #M13 已做，#m13 等同已完成，跳过）/ #m15 Toast 最大堆叠数 / #m16 ArmorParticles.vue 删除或接入

## 关键约束（来自项目记忆，必须遵守）

- `cd frontend && npm run build` 必须 0 错误（warnings 可接受，但 three.js chunk > 500kB 警告是库本身大小已按 #M8 拆出符合预期，可忽略）
- 不影响既有"秉烛研卷"视觉主题（墨色底 #07101c + 烛光琥珀 #e8b974 + 毛笔标题 + 诗句点缀），不引入 AI 感样式
- 不破坏第 1 批已通过的 11 项改动（保留工作区原状，只增量修改）
- 定制化捡漏雷达 UI 需含稀缺提示、价格锚定（¥9.9 vs ¥99）、信任背书
- 解锁完整报告触发粒子消散动画（如未实现则不阻塞本次任务）
- 不碰后端代码，契约类问题在前端只做防御性消费
- 不要 commit/push，审核员验收通过后会代执行 git 操作

## 完成后必做（协作闭环）

1. 跑自检：`cd frontend && npm run build` 必须 0 错误
2. 在 `/Users/sanzhaibanniang/Claude/Projects/gaokao/协作中心/反馈/` 新建 `20260629_前端AI_完成说明_第二批_续.md`
   - 列明每项 #M4-#M16 + #m1-#m16 的改动文件路径 + 行号 + 关键代码片段
   - 附 build 输出片段证明 0 错误
3. 在 `/Users/sanzhaibanniang/Claude/Projects/gaokao/协作中心/时间线.md` 末尾追加（注意 [时间][对象] 前缀）：
   ```
   [2026-06-29 HH:MM:SS][前端AI] ✅ 完成第二批第 2 批 8 项 + 第 3 批 16 项，build 0 错误：
   - 🟡 #M4 ... (文件:行号)
   - 🟡 #M5 ...
   ...
   - 🟢 #m1 ... 
   ...
   详细完成说明见 [反馈/20260629_前端AI_完成说明_第二批_续.md](反馈/20260629_前端AI_完成说明_第二批_续.md)。
   24 项全部完成，触发审核员验收。
   ```
4. 完成后通知审核员验收（无需 commit/push，审核员会代执行）

## 兜底说明

- 第 1 批改动保留在工作区，不要回退、不要 stash、不要 commit
- 若某项因架构原因无法实现（如 #m12 死代码清理发现某项实际有引用），在完成说明中标注「保留 + 原因」即可，不算失败
- #M16 ESLint 配置若与现有代码风格冲突过多，可只加 typecheck 脚本 + eslint 配置文件骨架（不强制全量通过）

现在开始：先读任务清单，然后按 ROI 顺序执行第 2 批 8 项 → 第 3 批 16 项。完成后写完成说明 + 追加时间线。
```
