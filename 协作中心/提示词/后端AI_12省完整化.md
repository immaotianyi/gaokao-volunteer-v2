# 后端 AI — 12 省数据完整化 + 优化任务（单 AI 顺序执行）

> 下达时间：2026-06-27 12:40:00 | 下达人：审核员（用户授权）
> 背景：用户撤回多 agent 并发爬取策略，改为由你单一 AI 顺序完成 12 省数据爬取与完整化。
> 根因：多 agent 各自决策导致粒度不一致（福建 per-major vs 广东 per-专业组）、数据残缺（非广东 yifenyiduan 484-1612 应 4000+）、占位数据（川苏 409）。#14 已修竞态（raw→merge 管道就绪），但数据质量未补全。
> 决策：单 AI 顺序执行 = 一致性 + 安全。#13 事故验收结论见时间线 12:32:24。

## 你的角色与策略
你是【后端 AI】，独自完成 12 省数据爬取。优势：单一标准、粒度统一、无并发冲突。你已经建好 merge_all.py + raw 管道（#14），本次在此基础上补全数据。

## 12 省清单（用户原始指令）
辽宁、江西、湖南、湖北、河北、福建、重庆、安徽、四川、陕西、河南、甘肃
（广东已完整，只校验不重爬）

## 每省必须完成的 4 类数据
1. **yifenyiduan**（一分一段表）：2024 + 2025 + 2026，每年物理类 + 历史类，全分数段（100-700 每分一行）
2. **plans_2026**（招生计划）：专业组级粒度（与广东/甘肃一致，**禁止 per-major**），含 university_code/group_code/major_code/plan_count
3. **admission_history**（录取历史）：2024 + 2025 + 2026 投档线
4. **control_line**（省控线）：2024 + 2025 + 2026

## 执行顺序与硬性要求

### Phase 1 — 逐省顺序爬取（每省爬完即校验，过了才爬下一省）

对每个省份（按此顺序：甘肃→陕西→河南→四川→安徽→重庆→福建→河北→湖北→湖南→江西→辽宁）：

1. **真实源优先**：从该省考试院官网爬 PDF/HTML。**禁止模板法重建**（广东 plans 模板法是原数据丢失的无奈之举，其余省必须真实数据）。若某省某年 PDF 确实无法获取，标 TODO 跳过，不造假。
2. **粒度统一**：
   - plans：专业组级（groupby 院校+组码+科类，plan_count 求和）。福建已聚合到 3051 行是范本。
   - yifenyiduan：每分一行，含 segment_count + cumulative_count
3. **科类转换**：陕西/四川等 2024 若用"文科/理科"，按 _common_spec 规则转为"物理类/历史类"（文→历史、理→物理，分数段对应保留）
4. **写 raw 不写主**：所有输出写 `data/raw/{省}_{类型}.csv`（如 `data/raw/河南_yifenyiduan_2026.csv`），**禁止碰主 CSV**（#14 红线）
5. **幂等**：重跑同省先删 raw 中该省旧数据再追加（现有 append_to_csv 已实现，确认保留）
6. **每省即时校验**（过了才爬下一省）：
   - yifenyiduan：每省每年每科类 ≥ 400 行（分数 100-700 覆盖）
   - plans_2026：每省每科类 ≥ 300 行（专业组级）
   - admission_history：每省每年每科类 ≥ 200 行
   - control_line：每省每年 ≥ 4 行
   - score_rank 抽测：该省某分数→位次应为正整数且合理（非 0、非负、非百万级离谱值）
   - 不达标则重爬该省，不要带病进下一省

### Phase 2 — 全量重建 + 审计
1. 12 省全爬完后跑 `python backend/scripts/merge_all.py` 重建所有主 CSV
2. 跑 `python backend/data/_audit_completeness.py` 复核各省行数
3. **广东完整性回归**：广东 yifenyiduan_2026 仍须 8816、plans_2026 仍须 4970（不能被 merge 破坏）
4. score_rank 多省抽测：广东 600→3367、河南 600→32622、再抽 2 省核验位次合理性
5. 重启 uvicorn 验证加载无错

### Phase 3 — 闸门硬化（#20）
数据补齐后，改 `backend/scripts/merge_all.py` L186-194：
- 把 `# 标记但不阻塞` 改为硬失败（行数不足 → return False，阻止写入主 CSV）
- 注释更新为"数据已完整化，行数不足视为事故，阻塞合并"
- 重跑 merge_all.py 确认闸门能拦截（故意删一行 raw 测试是否 block）

### Phase 4 — 代码优化（多省上线后必修）
1. **选科稀缺度省份自适应**（[leakage_radar.py#L68](file:///Users/sanzhaibanniang/Claude/Projects/gaokao/backend/services/leakage_radar.py#L68)）：当前 `SUBJECT_COMBO_RATIO` 是单一全国字典。改为按 province 查表（无该省数据时回退全国平均），各省份实际选科比例不同（如河南物化捆绑率 ≠ 广东）。
2. **新校区数据扩展**：`new_campuses.json` 当前仅 7 条（项目硬约束要求扩展）。爬取各省新增校区/分校区信息补充至 ≥30 条。
3. **plans_2025 多省补全**：当前仅粤甘闽皖 4 省，按 plans_2026 反向生成 plan_count_prev 补齐 12 省。
4. **残缺省份清理**：admission_history 中 海南6/上海264/内蒙古182 若无法补全则删除（避免残缺数据误导），安徽76 必须补全（在 12 省清单内）。

## 验收标准（全过才算完成）
- [ ] 12 省 × 4 类数据全到位，每省过 Phase 1 即时校验
- [ ] merge_all.py 一次性重建，广东完整性回归（8816/4970）
- [ ] _audit_completeness.py 全绿（无非广东 < 阈值）
- [ ] score_rank 4 省抽测位次合理
- [ ] #20 闸门硬化，能拦截残缺数据
- [ ] 选科稀缺度省份自适应上线
- [ ] new_campuses.json ≥30 条
- [ ] plans_2025 12 省齐

## 协作闭环
1. 每完成 3 省在时间线写一次进度：`[时间][后端AI] 🔄 已完成 {省1}/{省2}/{省3}，剩余 N 省`
2. 全部完成后：`[时间][后端AI] ✅ 12 省数据完整化 + 优化全部完成`
3. 归档 `协作中心/反馈/20260627_后端AI_12省完整化说明.md`，列出每省每类行数 + score_rank 抽测结果
4. 遇某省 PDF 无法获取，时间线写 `[时间][后端AI] ⚠️ {省}{类型} 源缺失，标 TODO 跳过`，不要造假数据

## 红线
- 任何 sync 脚本直接写主 CSV = #13 事故复发
- 禁止模板法/占位法造假数据（广东 plans 除外，已说明）
- 广东数据只校验不动（8816 yifenyiduan 是项目命根）
- 每省爬完不校验就爬下一省 = 带病推进，返工成本翻倍

## 优先级
若时长紧张，按此顺序保底：甘肃→陕西→河南→四川（用户原始 P0 四省）→ 其余。但最终目标仍是 12 省全齐。
