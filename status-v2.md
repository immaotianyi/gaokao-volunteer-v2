# 高考志愿狙击手 — 全局缺陷地图 v2

最后更新: 2026-06-15  
修复进度: P0 ✅ | P1 ✅ | P2 待启动 | P3 ✅

---

## 修复全貌

### 已完成 (7/12)

| 优先级 | 编号 | 问题 | 修复方式 |
|--------|------|------|------|
| P0 | — | mock_server 不读 enrollment_rules.json | 替换为 `risk_checker._mock_check`，接入 KB |
| P1 | 1.1 | body_check 字符串格式不兼容 | `_parse_body_check_string()` + `enrollment_kb` null 加固 + `{"raw":...}` 中间格式 |
| P1 | 1.3 | low_preference 几乎空白 | 专业名关键词扫描+分类注入 (3→65条, 2→41校) |
| P1 | 2.1 | low_preference 不被消费 | KB low_preference 消费 + 通用关键词兜底 |
| P1 | 2.3 | 随机英语推断 | 移除 `random.random()`，改为确定性 PASS |
| P3 | — | 前端无法渲染 | UniApp→Vue3 改造 + rpx→px + 残留文件清理 |
| P3 | — | Vite 3000端口启动 + HTML返回正常 | npm install + 路由编译验证 |

### 部分修复 (1/12)

| 优先级 | 编号 | 问题 | 进展 | 剩余缺口 |
|--------|------|------|------|------|
| P1 | 1.2 | single_subject 空洞 | 正则扫描章程全文 txt，注入全校通配规则 (10→36条, 8→21校) | 约 115/135 校的章程单科门槛因句式复杂未被正则提取，需后续 LLM 扫描 |

### 已搁置 (4/12)

| 优先级 | 编号 | 问题 | 搁置原因 |
|--------|------|------|------|
| P1 | 2.2 | 章程全文 txt 未在 mock 路径利用 | 已做正则提取（搭了第一步桥），全文注入需 LLM 调用链路，mock 路径暂无 LLM |
| P2 | — | 历史分数线缺失 (lowest_score/rank 全空) | 数据采购/爬取，非代码修复 |
| P2 | — | 选科概率计算重叠 (SUBJECT_COMBO_RATIO) | 数值修复，不影响核心链路 |
| P2 | — | 新校区字典过小 (仅3条) | 数据补充，需人工调研 |

---

## 当前能力边界

### 探雷器已知覆盖
- **体检 (色盲/色弱/身高/视力)**: 135 校全覆盖，4 级风险 (DANGER/WARNING/PASS)，兼容 dict/str 双格式
- **单科门槛**: 21 校，36 条规则（正则提取 + 人工标注 field/threshold）
- **低偏好标记**: 41 校，65 条规则（关键词扫描 + 2 校人工标注）
- **语种限制**: 全量检测
- **章程兜底**: 未收录大学 → 通用体检指导意见

### 探雷器已知盲区
- 章程全文 txt 中的隐性规则（复杂句式、非标准表述）未被提取
- 正则无法提取的单科门槛（约 100+ 所大学的章程未被覆盖）
- 专业组内的调剂风险（同组有其他低偏好专业）——无法计算
- 没有"身高限制"的完整数据（仅乐山师范学院有标注）

### 雷达已知状态
- 静态 mock 数据（8 条），未接 `leakage_radar.py`
- 估值模型因历史分数线缺失无法运行
- 纯净组过滤逻辑在 `leakage_radar.py` 中存在但未被调用

### 前端已知状态
- Vite + Vue3 + Router + Pinia 编译通过
- 三页面路由正常
- API 层使用 `localhost:8000` 硬编码
- 无生产构建测试
- 无移动端适配验证
